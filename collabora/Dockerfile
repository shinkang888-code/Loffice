# Collabora Online (LibreOfficeKit) — Render Docker
FROM collabora/code:24.04.13.3.1

# Render sets PORT; Collabora listens on 9980 internally.
# Environment (domain, aliasgroup*, username, password, extra_params) is injected by Render.
